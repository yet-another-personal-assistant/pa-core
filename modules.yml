telegram:
  command: .env/bin/python3 single_stdio.py
  cwd: tg
  type: stdio
brain:
  command: sbcl --noinform --load run.lisp --eval '(com.aragaer.pa-brain:main)' --quit
  type: socket
  cwd: brain
translator:
  command: .env/bin/python3 translator.py
  type: socket
  socket: /tmp/tr_socket
  cwd: pa2human
